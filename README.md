 When should I use a Struct & Record instead of a class? 
 -Use **class** when you need complex objects that change often and contain behavior (methods).
 -Use **struct** for small, simple objects that represent a value and do not change frequently.
 -Use **record** when you want to store data only and compare objects by value instead of reference.
